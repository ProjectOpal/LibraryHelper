dist: trusty
sudo: required
language: node_js
node_js:
  - '4'

install:
  - sudo apt-get update
  - sudo apt-get install expect
  - npm install -g particle-cli

script:
  - chmod +x particle-cli-login.sh
  - ./particle-cli-login.sh $email $pass
  # - particle compile photon
